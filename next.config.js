/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['picsum.photos', 'promex-dev-files-2025.s3.amazonaws.com'], // Add external image domain here
    },
};

export default nextConfig;
